import 'package:flutter/cupertino.dart';

Widget hSpacer({double mHeight = 20}) {
  return SizedBox(
    height: mHeight,
  );
}

Widget wSpacer({double mWidth = 20}) {
  return SizedBox(
    width: mWidth,
  );
}
